#!/usr/bin/env ruby
$: << File.join(File.dirname(__FILE__), '..', 'lib')

require 'bundler/inline'

gemfile do
  source 'https://rubygems.org'

  gem 'args_parser'

  gem 'rspotify'
  gem 'ruby-mp3info'

  gem 'pry'
end

require './lib/app.rb'
